# 将棋クイズ

将棋のクイズアプリ。**囲い・手筋・戦法・知識/格言・1手詰** の全 119 問を、
実際に動く盤とパラパラ漫画で出題する。おもな相手は小中学生なので、
ふりがな多め・文字大きめ・タップしやすさ優先。

- **囲いクイズ** … 盤の形を見て囲いの名前を当てる (27種)
- **手筋パラパラ** … 手筋が決まるまでを動画で見せて名前を当てる (15種)
- **戦法パラパラ** … 平手初形からの出だしを1手ずつ再生して戦法を当てる (19種)
- **1手詰クイズ** … 局面を見て、詰む手を4択から選ぶ (10題)
- **知識・格言** … ルール、駒の動き、戦法、ことわざ (48問)
- **ミックス十番勝負** / **ずかん** (全部を動く絵つきで見られる)

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックとクイズの中身のテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
npm run build      # 1枚の HTML に焼く (テストプレイ用 / Artifact 用)
npm run icon       # icon.png から apple-touch-icon.png / icon-192.png / icon-512.png を焼き直す
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面
core.js                   将棋のルール + 出題の組み立て。DOM を触らない
data.js                   クイズの中身 (囲い・手筋・戦法・知識・詰将棋)
app.js                    盤の描画、パラパラ再生、画面の進行
core.test.js              ルールのテスト (node --test)
data.test.js              クイズの中身を将棋のルールで検算するテスト
browser-test.js           ブラウザで動かすテスト
build-single.js           1枚の HTML に焼く
make-icon.js              icon.png から各サイズのアイコンを焼く
serve.js / sw.js          開発用サーバー / サービスワーカー
fonts/OFL.txt             駒の字 (Shippori Mincho) のライセンス
```

ファイルはすべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。増えてきたら
`src/` などに分けてよい (そのときは上の表と読み込み先も直す)。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。
`shogi-quiz-dojo.html` と `dist-artifact.html` は `npm run build` が作る出力なので、
直接さわらない (直すのは元のファイルのほう)。

---

## 決めごと

- **クイズの中身は、将棋のルールで検算する。** 戦法の手順・手筋の手順・詰将棋は
  ぜんぶ `data.test.js` が実際に指してみて確かめる。指せない手、二歩、
  「詰んでいない詰将棋」、「まちがい選択肢のはずが詰んでいる」はテストが落とす。
  **人が目で見て確かめない。** これが `core.js` に将棋エンジンを持っている理由
- **棋譜はふつうの書き方で書く。** `'7六歩'` `'同銀'` `'5二金打'` `'2二角成'`。
  どの駒が動いたかは盤から探すので、書くほうは棋譜どおりでよい。
  同じ場所へ行ける駒が 2 枚あるときだけ `'5八金(49)'` と元の場所を書く
  (書かないと「どの駒か決まらない」とテストが落ちる = 書きまちがい防止)
- **局面は 1 行で書く。** `'v玉51 ^銀53'` … `^` が先手、`v` が後手、駒 + 筋 + 段
- **見た目は和紙の白でいく。** 白い紙 × 墨の字 × 朱のさし色。筆文字のアイコンと、
  棋譜ノートに色をそろえてある。明るい画面でも暗い画面でも同じ見た目にする
  (子どもが使うので、端末の設定で色が変わらないほうが迷わない)。
  色は `styles.css` の `:root` にまとめる
- **見出しは明朝、本文は丸ゴシック。** 見出し・駒・棋譜は筆に近い明朝で和の顔を作り、
  読ませる文は丸ゴシックにする。絵文字は使わない (和紙の中で浮くため)。
  モードの札も、駒と同じ五角形に漢字を一文字のせている
- **駒の位置は `left` / `top`。** 動かすのは `element.animate()`。理由は下の表
- **盤と駒は「棋譜ノート」(`kakugawari/kifu`) に合わせる。** 同じ人が使うので、
  見た目と作法をそろえる。取り込んだのは次の 4 つ:
  - 駒の字は **Shippori Mincho の14文字だけを woff2 に切り出して埋めこむ** (3.6KB)。
    端末のフォントに左右されず、オフラインでも同じ形になる。
    ライセンスは SIL OFL 1.1 (`fonts/OFL.txt`)。作りなおしは棋譜ノートの
    `tools/make-koma-font.sh` と同じ手順
  - **盤は横幅いっぱいまで大きく。** 駒が読めるかどうかは、ほぼ 1 マスの大きさで決まる
  - 直前の手は、**もといたマスを桃色に塗り、動いた駒はふちだけを光らせる。**
    行った先までベタ塗りにすると駒の字が読めなくなる
  - 五角形は切り抜き (`clip-path`) なので枠線が引けない。**ふち用の五角形を下に重ねる**
- **囲いの盤は 5×5 に切り取って見せる。** 戦法は 9×9 全部。
  手筋は駒と手の行き先が入る四角を `core.cropFor()` が自動で決める

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: パズルなら、答えから逆算して問題を作れば必ず解ける)
- **ロジックを画面から切り離す。** `core.js` は DOM を触らない。
  だから node でテストできる。テストできる形にしておくと、あとが全部楽になる
- **ビルドを挟まない。** 素の HTML / CSS / JS。動かすまでの手数が少ないほど、
  確かめる回数が増える

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 「なんか遅い気がする」に対しては、
  1 フレームずつ位置や時間を記録して数字を出す。憶測で直さない
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと、
  ずれていても差が出ず見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** CPU を 4 倍遅くして計測すると差が見える

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる(CSS も JS も埋め込む)
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す。**
  ブランチ一覧と CI の実行履歴を見れば分かることを、憶測で説明しない

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めのインライン `transform: translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| まわすと指の下から対象が逃げる | 回転で縦横が入れ替わり、並び全体がずれていた | 入れ物を正方形にして、回しても大きさを変えない |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| アイコンを差しかえてもホーム画面が変わらない | 1枚ものに焼くとき `apple-touch-icon` の行ごと消していて、公開先に PNG も置いていなかった。iOS は data: の絵を受けつけないので、埋めこみでは代用できない | 公開用にはこの指定を残し、PNG を `index.html` の隣に置く |
| 1枚ものの HTML が急に重くなる | favicon に原寸 (1024px) のアイコンをそのまま埋めこんでいた | 埋めこむのは小さいサイズ (`icon-192.png`) にする |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みはまとめる。描き直しは変わったぶんだけ |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |
| 補助機能が途中で使えなくなる | 手もとにあるものしか探していなかった | すでに使われているものも回収の対象にする |
| 隠したはずの再生バーが出たまま | `hidden` 属性より `display: flex` のほうが強い | `[hidden] { display: none !important; }` を先に置く |
| 盤が豆つぶのように小さくなる | `align-items: center` の中で `width: 100%` を指定した。親の幅が中身で決まるので、たがいに縮み合う | 入れ物に `width: 100%` を持たせて、その中で中央ぞろえする |
| 「1手すすむ」を押しても動かない | 前の手を動かしている最中は、次の手を捨てていた | 手を待ち行列につないで、順番に動かす |
| 更新の確認テストが空ぶりする | `<title>` と `<h1>` に同じ文字列があり、`<title>` のほうを書きかえていた | 置きかえる範囲をタグごと指定する |
| 盤の大きさの上限が効かない | `max-width: calc(var(--cols) * 70px)` の `--cols` を、子要素にしか置いていなかった | 使う側の要素にも同じ変数を置く |
| 駒の字が下で切れる | マスいっぱいまで大きくしたうえに `padding-top` で押し下げていた | 字は 1 マスの 0.52 倍、押し下げは 5% までにする |

**新しく踏んだら、この表に足すこと。**
