# このリポジトリと、アプリの作り方

<!-- ここから: 新しいアプリを始めたら、この節を書き換える -->

**まだ何も作っていない雛形の状態。** 最初の作業で、ここに
「何のアプリか」「どう動かすか」「決めごと」を書き足すこと。

<!-- ここまで -->

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面
core.js                   ロジック。DOM を触らない。ブラウザと node の両方で動く
app.js                    操作・描画
core.test.js              ロジックのテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
```

ファイルはすべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。増えてきたら
`src/` などに分けてよい (そのときは上の表と読み込み先も直す)。

`window.__app` はテストから中身をのぞくための入口。`test/browser.js` が使う。

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: パズルなら、答えから逆算して問題を作れば必ず解ける)
- **ロジックを画面から切り離す。** `src/core.js` は DOM を触らない。
  だから node でテストできる。テストできる形にしておくと、あとが全部楽になる
- **ビルドを挟まない。** 素の HTML / CSS / JS。動かすまでの手数が少ないほど、
  確かめる回数が増える

### スマホで触るときの決めごと

- **連打で画面が拡大しないようにする。** `html, body` に
  `touch-action: manipulation` を置く。ボタンを続けて押したいときに
  ダブルタップと取られて拡大されると、操作にならない
- **ただしピンチでの拡大は残す。** `user-scalable=no` は書かない。
  文字を大きくして読みたい人が読めなくなるため (iOS はそもそも無視する)
- 横になぞって操作したい所だけ、その要素に `touch-action: pan-y` を置く

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 「なんか遅い気がする」に対しては、
  1 フレームずつ位置や時間を記録して数字を出す。憶測で直さない。
  **どこが遅いかを数えてから直す。** 思いついた所から直すと、まず外す
  (1 回の配置し直しに何 ms かかるか、何回起きているかを数える)
- **偽の入力は、値が動くものを使う。** 無音のマイク・空のデータ・0 の座標で
  試すと、動いていないのに全部通ってしまう。拾えた値が 0 でないことも見張る
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと、
  ずれていても差が出ず見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** CPU を 4 倍遅くして計測すると差が見える

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる(CSS も JS も埋め込む)
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す。**
  ブランチ一覧と CI の実行履歴を見れば分かることを、憶測で説明しない

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めのインライン `transform: translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| まわすと指の下から対象が逃げる | 回転で縦横が入れ替わり、並び全体がずれていた | 入れ物を正方形にして、回しても大きさを変えない |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みはまとめる。描き直しは変わったぶんだけ |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |
| ボタンが押せない。閉じているはずの層が全面をふさぐ | `display` を持つ要素に `hidden` を付けても効かない。`display: flex` が `[hidden]` に勝つ | `[hidden] { display: none !important }` を一箇所で効かせる (雛形に入れてある) |
| 画面に見出しが一つも無い | 全画面のアプリにしたとき `<h1>` を置き忘れる。読み上げ支援が構造を辿れず、更新確認のテストも落ちる | 目に見せなくてよいので `.sr-only` で置く (雛形に入れてある) |
| 一覧で項目を開くと、押した項目が指の下から逃げる | 開いていた別の項目を自動で閉じており、その高さぶん下が繰り上がっていた。画面のいちばん上にいると、巻き戻して打ち消す余地も無い | 自動で閉じない。複数を開いたままにできるようにする |
| 切り替えた直後、画面と中身が食い違う | アニメーションの `onfinish` で中身を差し替えていた。続けて操作すると、前の中身のまま測ってしまう | 中身の差し替えを先に済ませ、**見た目だけ**をあとから動かす |
| 主役が縮んで読めなくなる | 情報を足すとき、主役の下に積んだ。そのぶん主役の高さが減る | 重ねる。場所を取らないので、主役の大きさが変わらない |
| 入れ物が縮んだのに、中の文字がはみ出す | 文字の大きさを測り直していなかった。画面の回転しか見張っていなかった | 何が変えたかを数え上げず、`ResizeObserver` で入れ物そのものを見張る |
| はみ出しているのに「収まっている」と判定する | 採寸が**高さしか見ていなかった**。折り返せないもの (長い `<ruby>` など) は横に伸びる | 採寸は幅も見る |
| 操作のたびに引っかかる | 見えていない側の中身が、ずっと配置計算に入っていた。さらに初めて出すものを、そのつど測り直していた | 見えていない側は `content-visibility: hidden` で計算から外す。この先の分は手が空いた隙に先に測っておく |
| 先まわりの処理が、途中までしか進まない | 手が空いた隙にやる作りで、時間切れになったとき打ち切ったままにしていた | 時間切れなら、また手が空いたときに続きをやる |
| 並べ替えが偏る | `sort` に乱数を返す比較関数を渡していた | Fisher-Yates で混ぜる (`core.js` の `shuffle`)。どの要素もどの位置に来るかをテストで確かめる |
| サービスワーカーの更新確認が、直したのに落ちる | テストが `<h1>` の**中身の文字だけ**を置換しており、同じ文字が手前の `<title>` にもあってそちらに当たっていた | タグごと置き換える (雛形のテストは直してある) |
| 外の書体が取れない環境で、テストが「画面のエラー」で落ちる | 読み込み失敗のメッセージ本文に URL が入らないので、本文だけ見ても見分けられない | `console` の**発生場所**の URL で見分け、書体の失敗だけ別扱いにする (雛形のテストに入れてある) |
| 偽の入力でテストしたら、全部ゼロのまま通った | 無音・空・0 を流していたので、動かないのに合格していた | 値が動く入力を作って流し、拾えた値が 0 でないことを見張る |
| ボタンを続けて押すと、画面が拡大してしまう | ブラウザがダブルタップと取って拡大する。指の操作を邪魔する | `html, body` に `touch-action: manipulation`。ピンチでの拡大は残す (`user-scalable=no` は書かない) |
| 補助機能が途中で使えなくなる | 手もとにあるものしか探していなかった | すでに使われているものも回収の対象にする |

**新しく踏んだら、この表に足すこと。**
