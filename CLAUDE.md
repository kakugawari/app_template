# このリポジトリと、アプリの作り方

<!-- ここから: 新しいアプリを始めたら、この節を書き換える -->

**強直性脊椎炎のリハビリ用ストレッチアプリ。** 決まった種目を、順番に、
`はじめる → できた → できた → …` の一本道でこなしていくだけのアプリ。

- 種目は今のところ `core.js` の `DEFAULT_EXERCISES` に決め打ちで入っている
  (11種目・左右ぶんで計17ステップ、すべて20回)。編集画面はまだ無い
- 並びは姿勢ごと。寝る → 四つん這い → 立つ → 座る の順にして、
  途中で体勢を変える回数を減らしている
- イラストは種目ごとの `art` (名前) で引く。`ex0` のように並び順で引くと、
  種目を足すたびに全部ずれる
- 回数はアプリ側では数えない。「20回」と表示するだけで、終わったら
  自分で「できた」を押して次へ進む(運動中はスマホを触りづらいため)
- 1 周やりきるとスタンプが 1 個たまる。10 個でカードが 1 枚うまり、
  ごほうび (色・人の飾り) が 1 つ増える。記録は localStorage に置く
- 途中でやめた位置も覚えていて、次に開くと「つづきから」が出る。
  スタンプの記録とは別のキーに分ける (途中の位置が壊れても、
  積み上げたスタンプまで巻き添えにしないため)
- 実行中は Wake Lock で画面を消さない。20 回やっているあいだは
  スマホを触れないので、放っておくと必ず消灯する
- 持っているのは「通算いくつ押したか」だけ。カードの枚数も、もらった
  ごほうびも、そこから毎回計算する。2 か所に数を持つと必ずずれる
- 継続日数の可視化はまだ無い。次にやるとしたらここ
- 見た目はアメリカンヴィンテージ(クリーム紙 + ラスト + マスタード + ネイビー、
  スタンプ風バッジ)。色は `styles.css` の `:root` にまとめてある

<!-- ここまで -->

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面
core.js                   ロジック。DOM を触らない。ブラウザと node の両方で動く
app.js                    操作・描画
core.test.js              ロジックのテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
sw.js                     電波が無くても開けるようにする。ネット優先
manifest.json             ホーム画面から独立したアプリとして開くための設定
icon.svg                  アイコンの元。直すのはここだけ
make-icons.js             icon.svg から PNG を書き出す (npm run icons)
apple-touch-icon.png      ホーム画面用 (180)。以下 3 つは書き出したもの。手で直さない
icon-192.png / icon-512.png
```

ファイルはすべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。増えてきたら
`src/` などに分けてよい (そのときは上の表と読み込み先も直す)。

`window.__app` はテストから中身をのぞくための入口。`test/browser.js` が使う。

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: パズルなら、答えから逆算して問題を作れば必ず解ける)
- **ロジックを画面から切り離す。** `src/core.js` は DOM を触らない。
  だから node でテストできる。テストできる形にしておくと、あとが全部楽になる
- **ビルドを挟まない。** 素の HTML / CSS / JS。動かすまでの手数が少ないほど、
  確かめる回数が増える

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 「なんか遅い気がする」に対しては、
  1 フレームずつ位置や時間を記録して数字を出す。憶測で直さない
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと、
  ずれていても差が出ず見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** CPU を 4 倍遅くして計測すると差が見える

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる(CSS も JS も埋め込む)
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す。**
  ブランチ一覧と CI の実行履歴を見れば分かることを、憶測で説明しない

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めのインライン `transform: translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| まわすと指の下から対象が逃げる | 回転で縦横が入れ替わり、並び全体がずれていた | 入れ物を正方形にして、回しても大きさを変えない |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| ホーム画面のアイコンの角に黒い額縁が出る | 角を丸めた PNG を渡していた。iOS は受け取った画像の角を自分で丸めるので、こちらで丸めたぶんの透明部分がそのまま黒く塗られる | PNG は角を丸めず正方形のまま書き出す (`make-icons.js` が `icon.svg` の `rx` を外している)。ブラウザテストで四隅の alpha が 255 か見張る |
| アイコンを直したのに、ホーム画面のだけ古いまま | SVG だけ直して PNG を書き出し直していなかった | 元は `icon.svg` 1 つに絞り、PNG は必ず `npm run icons` で作り直す |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みはまとめる。描き直しは変わったぶんだけ |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |
| 補助機能が途中で使えなくなる | 手もとにあるものしか探していなかった | すでに使われているものも回収の対象にする |
| `hidden` 属性をつけたのに要素が消えない (複数画面が同時に見えてしまう) | 同じ要素に `display: flex` などを指定する CSS ルールが、`[hidden]` の `display: none` を上書きしていた (author の通常ルールは UA スタイルより常に優先されるため、順序やセレクタの詳細度に関係なく起きる) | `[hidden] { display: none !important; }` を1つ書いて一括で勝たせる。要素ごとに気をつけない |
| `el.hidden` で見た目を確認したつもりが、実は画面に出たままだった | `el.hidden` は属性の有無を返すだけで、実際に描画されているかは見ていない | ブラウザテストでは `getComputedStyle(el).display` など、本当の見え方を確認する |
| ある要素の中身が変わっただけなのに、画面全体がガクッと動く | 縦中央寄せ (`justify-content: center`) の中で、バッジの表示/非表示のように `display: none` で高さごと出し入れする要素があると、カード全体の高さが変わって再センタリングされ、無関係な要素まで動いて見える | 高さを保ったまま隠したいものは `display: none` ではなく `visibility: hidden` にする (要素は残り続けるので高さが変わらない) |
| 種目を1つ足しただけで、関係ない種目のイラストが総ずれした | イラストを並び順 (`ex0`, `ex1`, …) で引いていた。テストも「5回できたを押すと6種目め」のように順番を数えていた | 種目ごとに `art` の名前を持たせ、`<art>-<コマ名>` で引く。テストも数ではなく名前で目当てのステップまで進める |
| 一部のステップだけ文字がずれ、iOS では前の描画が残って二重に見えた | バッジの高さを中身まかせにしていた。`line-height` を決めていないと行の高さが字の形で決まるため、日本語が入るステップだけ 2px 高くなり、カードごと下の要素が動く。`visibility: hidden` で場所は取っていても、**高さが同じとは限らない** | 出入りする要素は `line-height` や `min-height` で高さを決め打ちし、中身に関係なく同じ箱にする。全ステップを歩いて高さが 1 種類しかないことを見張る |
| 図形が重なって「頭が消える」のに、座標を見比べても異常が見つからない | 回転させた棒の**端**が別の図形に乗り上げていた。中心どうしの距離や、まん中の縦一列だけを見ても、端の当たりは通らない | 実際に描かせて (canvas に落として) 当たってほしくない場所に墨が入っていないかを数える。見張りを足したら、**わざと壊して落ちることまで確かめる** (今回、最初に書いた見張りは元の不具合を素通りしていた) |
| キーフレームの時刻を細かく決めたのに、思った間合いにならない | `element.animate()` の timing に `easing` をまとめてかけると、進み方そのものが曲がり、`offset` が実時間とずれる。「0.12 まで大きいまま」のつもりが、実際は数十ミリ秒で通り過ぎていた | 間合いを自分で決めたいときは timing 側を linear のままにし、`easing` はキーフレーム 1 つずつに持たせる。アニメを `pause()` して `currentTime` を指定し、狙った時刻の絵を実際に撮って確かめる |
| アニメの途中を撮ろうとすると、いつも終わった後の絵になる | Playwright の `locator.screenshot()` は要素が「安定する」まで待つので、動いている最中は撮れない。時間で待って撮る方法も、撮影自体に数十ミリ秒かかるため必ず撮り遅れる | `page.screenshot({ clip })` を使い、`document.getAnimations()` を `pause()` して `currentTime` で時刻を指定してから撮る |
| 「動きを減らす」設定にしているのに、絵が動き続ける | `@media (prefers-reduced-motion: reduce)` の `animation: none !important` は **CSS アニメーションにしか効かない**。`element.animate()` (Web Animations API) で動かしたものは止まらない | JS 側でも `matchMedia('(prefers-reduced-motion: reduce)').matches` を見て、動かさない道を用意する。ブラウザテストは `reducedMotion: 'reduce'` の context で `document.getAnimations().length === 0` を見張る |
| 絵を大きくしたら、横向きのときに主要なボタンが画面の外に出た | 縦が短い画面 (横向きのスマホ) を試していなかった。縦の余白は積み上がるので、1 か所大きくすると下の要素が押し出される | `@media (max-height: ...)` で詰める。ブラウザテストで「ボタンの下端 ≤ 画面の高さ」を見張る |
| SVG の `<use>` で呼んだ図形に CSS の色が当たらない (塗りが黒のままになる) | `<use>` の中身は影の木に複製されるため、`.illustration .ball` のように祖先をたどるセレクタは中まで届かない | クラス単独 (`.ball { … }`) で書く。または `fill` が継承される性質を使い、`<use>` 側にまとめて指定する |
| 途中まで進んだところで画面を切り替えて戻ると、そこから先は画面が消えるようになる | Wake Lock は画面を閉じたりタブを移ると**ブラウザが勝手に解除する**。持っているつもりで握り直さないと、以降ずっと効かない | `visibilitychange` で戻ってきたときに取り直す。解除されたときに参照も捨てる (`lock.addEventListener('release', …)`) |
| ボタンを 1 つ足しただけで、狭い画面で下のものが画面の外に出た | 幅が足りないとボタンの**中で**文字が折り返し、ボタン自体が 44px → 94px に伸びていた。横に入らないぶんが縦に化けていた | `white-space: nowrap` で折り返させず、狭い画面では字と余白を詰める。ボタンの段の高さを見張る (幅ではなく高さで気づける) |
| ボタンを続けてタップすると、画面ごと拡大されてしまう | `touch-action` を指定していなかったので、ブラウザが「ダブルタップで拡大」の操作だと勘違いしていた | `html, body { touch-action: manipulation; }` でダブルタップ拡大だけを止める。ピンチズームは残るので、文字を大きくしたい人の邪魔にはならない |
| 「できた」を連打すると、ストレッチしていなくても一瞬で何ステップも進み、通算スタンプまで実際より多くつく | ボタンは押されるたびに素直に 1 回ぶん進める作りだったので、連打した回数がそのまま反映されていた。実測: `btn.click()` を続けて呼ぶと index が 0 → 2 のように動いた | 押した直後は一定時間 (`el.disabled = true`) 押せなくする。次のセッションを始めるときに解除し忘れると、そのセッションの最初の「できた」まで固まって反応しなくなるので、`start()` 側でも明示的に解除する。ブラウザテストは Playwright の `locator.tap()` ではなく素の `click()` を待たずに連続で呼び、指が追いつかない速さの連打を再現する (`tap()` は要素が押せる状態になるまで待ってしまうため、それでは連打を再現できない) |

**新しく踏んだら、この表に足すこと。**
